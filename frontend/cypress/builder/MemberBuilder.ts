export class MemberBuilder {
  private data: any;

  constructor() {
    // Initialize with default values
    this.data = [];
  }

  // Method to build the final project object
  build() {
    return this.data;
  }
}
